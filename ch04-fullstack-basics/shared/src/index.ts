// ch04 shared types — re-exported from server schema
// In a real monorepo, shared types live here and are consumed by both server and web.
// For this chapter, the primary types (User, Todo) are defined in server/src/database/schema.ts.
export {};
